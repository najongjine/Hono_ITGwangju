t_user
회원/관리자 정보
구글 로그인한 사람의 이메일, 이름, 권한 같은 정보를 저장

t_courses
과정 종류
예: 웹디자인 과정, 컴퓨터활용능력 과정, 영상편집 과정

t_course_sessions
과정의 기수/회차
예: 웹디자인 과정 1기, 2기, 2026년 6월 개강반

t_enrollments
수강 신청 기록
누가 어떤 과정의 어떤 기수에 신청했는지 저장

t_apply
신청서 상세 내용
신청자가 입력한 이름, 연락처, 주소, 생년월일, 신청 내용 등

t_files
파일 원본 정보
업로드한 파일명, 저장 경로, Supabase Storage 주소, 파일 크기 등

t_file_links
파일 연결 정보
파일이 공지사항에 붙은 건지, 게시글에 붙은 건지, 과정에 붙은 건지 연결

t_notices
공지사항
학원 공지, 개강 안내, 휴무 안내 같은 공식 알림

t_posts
게시글
후기, 자료글, 일반 게시판 글 같은 콘텐츠

t_inquiries
문의
상담 신청, 질문, 답변, 처리 상태 저장


------------------------

(이건 구현 안함. 그냥 노트용)
요즘 서브메뉴 등록 방식은 잘 안한다함.
백앤드 일만 커짐

만약 진짜 한다면

과정안내
- 웹디자인
- 영상편집
- 컴퓨터활용능력
- 전산회계
- 자바 개발자

이정도만 추천











------------------------
SELECT
    c.table_schema AS schema_name,
    c.table_name,
    c.ordinal_position,
    c.column_name,
    c.data_type,
    c.character_maximum_length AS max_length,
    c.is_nullable,
    c.column_default
FROM information_schema.columns c
WHERE c.table_schema = 'public'
ORDER BY c.table_name, c.ordinal_position;


"table_schema"	"table_name"	"columns"

"public"	"t_apply"	"id integer NOT NULL, enrollment_id integer, user_id integer, session_id integer, applicant_name character varying NOT NULL, phone character varying NOT NULL, email character varying NOT NULL, birth_date date, gender character varying NOT NULL, address character varying NOT NULL, detail_address character varying NOT NULL, current_job character varying NOT NULL, education_level character varying NOT NULL, application_content text NOT NULL, privacy_agreed boolean NOT NULL, status character varying NOT NULL, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_course_sessions"	"id integer NOT NULL, course_id integer NOT NULL, session_name character varying NOT NULL, session_no integer, start_date date, end_date date, apply_start_date date, apply_end_date date, capacity integer NOT NULL, location character varying NOT NULL, status character varying NOT NULL, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_courses"	"id integer NOT NULL, course_name character varying NOT NULL, summary character varying NOT NULL, description text NOT NULL, thumbnail_file_id integer, is_visible boolean NOT NULL, status character varying NOT NULL, sort_order integer NOT NULL, created_by integer, updated_by integer, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_enrollments"	"id integer NOT NULL, user_id integer, course_id integer NOT NULL, session_id integer NOT NULL, applicant_name character varying NOT NULL, applicant_phone character varying NOT NULL, applicant_email character varying NOT NULL, apply_status character varying NOT NULL, approval_status character varying NOT NULL, memo text NOT NULL, applied_at timestamp without time zone NOT NULL, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_file_links"	"id integer NOT NULL, file_id integer NOT NULL, target_table character varying NOT NULL, target_id integer NOT NULL, file_role character varying, sort_order integer, created_at timestamp without time zone, updated_at timestamp without time zone"
"public"	"t_files"	"id integer NOT NULL, original_name character varying, stored_name character varying, file_path character varying, mime_type character varying, file_size bigint, created_at timestamp without time zone, storage_type character varying, bucket character varying, storage_key character varying, public_url character varying, updated_at timestamp without time zone NOT NULL, uploaded_by integer, status character varying NOT NULL"
"public"	"t_inquiries"	"id integer NOT NULL, user_id integer, name character varying NOT NULL, phone character varying NOT NULL, email character varying NOT NULL, title character varying NOT NULL, content text NOT NULL, answer text NOT NULL, answered_by integer, answered_at timestamp without time zone, status character varying NOT NULL, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_notices"	"id integer NOT NULL, title character varying NOT NULL, content text NOT NULL, author_id integer, author_name character varying NOT NULL, view_count integer NOT NULL, is_visible boolean NOT NULL, is_pinned boolean NOT NULL, status character varying NOT NULL, published_at timestamp without time zone, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_posts"	"id integer NOT NULL, category character varying NOT NULL, title character varying NOT NULL, content text NOT NULL, author_id integer, author_name character varying NOT NULL, view_count integer NOT NULL, is_visible boolean NOT NULL, status character varying NOT NULL, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
"public"	"t_test1"	"id integer NOT NULL, title character varying, content character varying, created_dt timestamp with time zone"
"public"	"t_test1_child"	"id integer NOT NULL, comment character varying, created_dt timestamp with time zone, test1_id integer"
"public"	"t_user"	"id integer NOT NULL, provider character varying NOT NULL, provider_user_id character varying NOT NULL, email character varying NOT NULL, name character varying NOT NULL, profile_image_url character varying NOT NULL, role character varying NOT NULL, status character varying NOT NULL, last_login_at timestamp without time zone, created_at timestamp without time zone NOT NULL, updated_at timestamp without time zone NOT NULL"
